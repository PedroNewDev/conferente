# Dependências do ambiente de demonstração serverless.
# Diferenças em relação a requirements.txt: sem psycopg (o demo usa SQLite),
# sem WeasyPrint (exige bibliotecas nativas GTK; o relatório sai em HTML) e
# sem uvicorn (a plataforma fornece o servidor ASGI).
fastapi>=0.110
jinja2>=3.1
sqlalchemy>=2.0
lxml>=5.0
passlib[bcrypt]>=1.7
bcrypt==4.0.1
pydantic-settings>=2.2
python-multipart>=0.0.9
itsdangerous>=2.1
apscheduler>=3.10
